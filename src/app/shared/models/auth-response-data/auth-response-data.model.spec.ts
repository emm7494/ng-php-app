import { AuthResponseData } from './auth-response-data.model';

describe('AuthResponseData', () => {
  it('should create an instance', () => {
    expect(new AuthResponseData()).toBeTruthy();
  });
});
