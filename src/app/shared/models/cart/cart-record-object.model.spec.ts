import { CartRecordObject } from './cart-record-object.model';

describe('CartRecordObject', () => {
  it('should create an instance', () => {
    expect(new CartRecordObject()).toBeTruthy();
  });
});
