import { CartRecordHeaderObject } from './cart-record-header-object.model';

describe('CartRecordHeaderObject', () => {
  it('should create an instance', () => {
    expect(new CartRecordHeaderObject()).toBeTruthy();
  });
});
